
var Brackets = function() {
    var self = {};

    self.handler = {};

    /**
     * Generates a bracket from an array of user objects
     * @param {Object[]} users
     * @param {string} users[].user_id
     * @param {number} users[].seed
     * @returns {Object} bracket
     */
    function generateBracket(users) {
        // var users = [
        //     {
        //         user_id: "adbeo23",
        //         seed: 1500
        //     }
        // ];
    
        // users[0].seed = 103;
        
    }


    return self;
}

module.exports = Brackets();